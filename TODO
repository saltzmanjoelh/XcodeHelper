-- Bugs --
Status menu should show "No Projects" as disabled in the menu when Xcode has no project
Manual Targets should show "No Targets" when disabled and no targets
Docker Build should be at the top
    Docker Build
    Automatic Target
    Manual Target ->
    -----------------


-- Features --
There should be a Console tab in the main window that shows output
    it should be html based so maybe just a webview
    errors are red, don't show empty error output like we currently do
